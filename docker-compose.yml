services: 
        studentapp: 
            build: ./studentapp
            ports:
                - "8080:8080"
        sample: 
            build: ./sample
            ports:
                - "5000:80"
            #volumes:
                #- .:/app
            #networks:
                #- host 
        #todsapp:
            #build: ../todsapp
            #ports:
                #- "9090:9090" 
            #volumes:
                #- ../todsapp:/app
            #networks:
                #- host        